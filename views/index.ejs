<h2>Kürzlich Hinzugefügte</h2>

<% books.forEach(book => { %>
    <!-- div><%= book.title %></div>
    <div>..<%= book.coverImagePath %></div>
    <div><%= book.coverImageName %></div-->
    <a href="/books/<%= book.id%>">
        <img height="150" width="100" src="<%= book.coverImagePath%>">   
    </a>
<% }) %>